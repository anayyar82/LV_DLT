resources:
  pipelines:
    lv_dlt_pipeline:
      name: lv_dlt_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "."
      libraries:
        - glob:
            include: transformations/**
